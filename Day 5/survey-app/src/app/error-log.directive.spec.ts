import { ErrorLogDirective } from './error-log.directive';

describe('ErrorLogDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorLogDirective();
    expect(directive).toBeTruthy();
  });
});
