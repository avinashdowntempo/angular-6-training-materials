import { SomeFilterPipe } from './some-filter.pipe';

describe('SomeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SomeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
